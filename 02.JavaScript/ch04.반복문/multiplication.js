//구구단(1~9단까지)

for(let i = 2; i <= 9; i++) {
    for (let k = 1; k <=9; k++){
        console.log(`${i} * ${k} = ${i*k}`);
    }
    console.log();      // 다음 단으로 넘어갈 때는 한 줄 띄어쓰기 됨 
}


//xxxxxxxxxxxxxx  아래는 오답 xxxxxxxxxxxxxxxxxxx */
//xxxxxxxxxxxxxx  아래는 오답 xxxxxxxxxxxxxxxxxxx */

/* let numbers = [2, 3, 4, 5, 6, 7, 8, 9];
let N = numbers[0];
for (let number of numbers)
    if (i = 0; i < numbers.length; i++);{
    continue;
    console.log(`index=${N}, multiple=${Sum}`);
}
 */

/* 
let numbers = [2, 3, 4, 5, 6, 7, 8, 9];
let num = numbers[0];
for(let num = numbers[0]);{
 if(i = 1; i <=9; i++)
}
console.log(`${num} * ${i} = ${num * i}`)

 */

/* let numbers = [2, 3, 4, 5, 6, 7, 8, 9];
let multiple = 0;
for(let num of numbers)
  for(i = 1; i <= 9; i++) {
    continue;
    num *= i;
}
   // console.log(`index=${i}, '*' , '=' value=${multiple}`);
   console.log(multiple); */
